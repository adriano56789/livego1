// Configurações de ambiente
export const ENV = {
  MOCK_API: true,  // Altere para false para usar a API real
  API_BASE_URL: 'http://localhost:3000/api',
  // Adicione outras variáveis de ambiente conforme necessário
};

export default ENV;
